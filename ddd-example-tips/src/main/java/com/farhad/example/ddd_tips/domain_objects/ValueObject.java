package com.farhad.example.ddd_tips.domain_objects;

// marker interface for all value objects. Implementations of this interface are required 
// to be immutable and implement equals() and hashCode(). 
public interface ValueObject extends DomainObject {
    
}
